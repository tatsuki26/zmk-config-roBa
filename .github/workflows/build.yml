on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to match config/west.yml's zmk revision (see comment there).
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@f9be3ed9c08b35b1b86df24c0f04913f3b5e50f5
