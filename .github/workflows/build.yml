on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    # Pinned to match config/west.yml's zmk revision (see comment there).
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@f9be3ed94301eaa5b9cf1bd63779cecc2c8b430e
