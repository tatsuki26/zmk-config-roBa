name: "Draw Keymap"

on:
  workflow_dispatch:
  push:
    paths:
      - "config/roBa.keymap"
      - "keymap_drawer.config.yaml"
      - "config/boards/shields/Test/roBa.dtsi"

jobs:
  draw:
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@f9cf9d3677368a7fb1dc505c51b8156fafa66fe2
    permissions:
      contents: write
    with:
      commit_message: "[Draw] ${{ github.event.head_commit.message || 'Update keymap drawing' }}"
      amend_commit: false
      keymap_patterns: "config/*.keymap"
      json_path: "config"
      config_path: "keymap_drawer.config.yaml" # config file, ignored if not exists
      output_folder: "keymap-drawer"
      destination: "both"
      # roBa.dtsi の物理レイアウト定義をファイル名自動検出(roBa-layout(s).dtsi)では
      # 見つけられないため、明示的に指定する
      draw_args: "roBa:'-d config/boards/shields/Test/roBa.dtsi'"
