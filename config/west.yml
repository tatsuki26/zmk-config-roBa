manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    - name: sayu-hub
      url-base: https://github.com/sayu-hub
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned to the last commit before ZMK's Zephyr 4.1 migration (2025-12-09),
      # which renamed boards (e.g. seeeduino_xiao_ble) and added a native
      # PMW3610 driver that conflicts with zmk-pmw3610-driver's devicetree
      # binding. Bump this once the trackball config is migrated to the
      # native driver. See https://zmk.dev/blog/2025/12/09/zephyr-4-1
      revision: abb64ba316c29caddc49727ca2cac2f0ed5970c7
      import: app/west.yml
    - name: zmk-pmw3610-driver
      remote: sayu-hub
      revision: main
  self:
    path: config
